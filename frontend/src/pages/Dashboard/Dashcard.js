import React from "react";

const Dashcard = (props) => {
  return (
    <div className="dashCard">
      <h1>{props.heading}</h1>
      <button></button>
    </div>
  );
};

export default Dashcard;
